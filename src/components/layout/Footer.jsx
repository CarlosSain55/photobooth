export default function Footer() {
  return (
    <footer>
      duo sessions connect two browsers directly using a public relay for introductions —<br />
      fine for sharing between friends; a production booth would want its own signaling &amp; TURN server.
    </footer>
  );
}
